# SAMZONE frontend

The frontend is documented in the repository [README](../README.md). Use
`npm run dev`, `npm run build`, and `npm run lint` from this directory.

Copy `.env.example` to `.env.local` for local development. Never place a
Supabase service-role key in a `VITE_*` variable.
